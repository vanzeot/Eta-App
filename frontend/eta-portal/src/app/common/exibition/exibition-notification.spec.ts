import { ExibitionNotification } from './exibition-notification';

describe('ExibitionNotification', () => {
  it('should create an instance', () => {
    expect(new ExibitionNotification()).toBeTruthy();
  });
});
